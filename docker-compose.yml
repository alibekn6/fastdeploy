services:
  app:
    build:
      context: .
      args:
        NEXT_PUBLIC_API_URL: https://api.example.com
    ports: ["3000:3000"]
    environment:
      NEXT_PUBLIC_API_URL: https://api.example.com
      NEXT_PUBLIC_API_MOCKING: "disabled"
