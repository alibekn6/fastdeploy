# pnpm blocks dependency build scripts by default. sharp (Next.js image
# optimization) ships a native binary built via a postinstall script, so allow it.
allowBuilds:
  cpu-features: true
  esbuild: true
  msw: true
  protobufjs: true
  sharp: true
  ssh2: true
