# fastdeploy

Опишите платформу обычными словами — получите работающий сайт в интернете.

> **Не программист? Вам сюда → [START_HERE.md](START_HERE.md)** — пошаговая инструкция
> с абсолютного нуля: Терминал, установка всего на Mac, первый запуск.

## Что это

Это стартовый набор для «вайбкодинга»: человек **без навыков программирования** открывает
эту папку в [Claude Code](https://claude.com/claude-code), пишет по-человечески, что ему
нужно — и получает настоящую платформу, а не набор HTML-страниц.

### Как это работает

| Шаг | Вы пишете | Что происходит |
| --- | --- | --- |
| 1 | «Хочу платформу, где клиенты записываются к мастерам» | Claude строит страницы, формы и логику внутри готового каркаса |
| 2 | «Покажи» | Сайт открывается в браузере — всё живое: вход, списки, записи |
| 3 | «Поменяй цвета, добавь страницу с ценами» | Claude меняет и показывает снова |
| 4 | «Опубликуй» | Сайт уезжает на Vercel — реальная ссылка, можно слать клиентам |

Нетехнарям больше ничего знать не нужно — начните с **[START_HERE.md](START_HERE.md)**.

### Почему это работает без бэкенда

Главный трюк — **образцовые данные (MSW)**. Бэкенда ещё нет, но платформа ведёт себя как
настоящая: вход по любому паролю, дашборды и списки наполнены данными. Так можно
спроектировать и показать заказчику весь продукт **до** того, как написана серверная часть.

Когда бэкенд с базой данных будет готов, переключение на реальные данные — это **одна
переменная окружения** (`NEXT_PUBLIC_API_MOCKING=disabled` + адрес API). Ничего из
построенного не переделывается. Бонус: все моки, которые Claude создал для фронта,
документируют API-контракт — команда бэкенда точно знает, какие ручки писать
(требования к бэкенду уже описаны в [docs/frontend-template.md](docs/frontend-template.md)).

### Что уже внутри каркаса

Страницы и роутинг · формы с валидацией · авторизация (по httpOnly-cookie, готова под
реальный бэкенд) · три языка (ru / kk / en) · тёмная тема · SEO (sitemap, robots, OG) ·
аналитика (PostHog, опционально) · тесты и линтеры — всем этим управляет Claude,
пользователю это не показывается.

## Скиллы

Управляемый опыт живёт в `.claude/skills/` и едет вместе с репозиторием:

| Скилл | Срабатывает на | Что делает |
| --- | --- | --- |
| `/start` | первая сессия, «с чего начать» | ставит всё нужное, создаёт `.env`, открывает сайт в браузере |
| `/build` | «хочу платформу…», «добавь страницу…» | строит функции по описанию обычными словами, по правилам шаблона |
| `/deploy` | «опубликуй», «выложи сайт» | деплоит на Vercel с правильными настройками (мок-режим, пока нет бэкенда) |

Пользователю не нужно знать эти названия — `CLAUDE.md` сам направляет обычные фразы в
нужный скилл. Слэш-команды — просто быстрый путь.

## Для разработчиков

```bash
pnpm install
cp .env.example .env   # затем поставьте NEXT_PUBLIC_API_MOCKING=enabled для мок-режима
pnpm dev:mock          # → http://localhost:3000 (вход с любыми данными)
```

| Задача | Команда |
| --- | --- |
| Дев-сервер (с моками / без) | `pnpm dev:mock` · `pnpm dev` |
| Линт / типы | `pnpm lint` · `pnpm typecheck` |
| Тесты (unit / integration / e2e) | `pnpm test` · `pnpm test:integration` · `pnpm e2e` |
| Storybook | `pnpm storybook` |
| Прод-сборка | `pnpm build` |

Стек: Next.js 16 (App Router) · React 19 · TypeScript strict · Tailwind v4 + shadcn/ui ·
Feature-Sliced Design (контроль Steiger) · TanStack Query · MSW · next-intl · Vitest + Playwright.

Глубокая документация (на английском — её читают разработчики и Claude):

- **[docs/frontend-template.md](docs/frontend-template.md)** — устройство шаблона:
  авторизация и куки, ограничения мок-режима, компромиссы безопасности и **требования к
  бэкенду** (прочитайте перед тем, как писать серверную часть)
- **[docs/stack/](docs/stack/)** — правила и лучшие практики по каждому инструменту
- **[CLAUDE.md](CLAUDE.md)** — инструкции для агента (конвенции, грабли, ревью-субагенты)

## Роадмап

- [x] Фронтенд-шаблон + скиллы для вайбкодинга + деплой на Vercel
- [x] Бэкенд внутри этого же Next-приложения: Neon Postgres (Drizzle), настоящий JWT
      (подпись HS256, argon2id, рейт-лимиты, httpOnly-куки — по OWASP и контракту
      шаблона). Включение — переменными окружения, фронт не меняется: см.
      [docs/backend.md](docs/backend.md)
- [ ] Расширение бэкенда под фичи платформ (вайбкодинг таблиц и ручек через `/build`)
